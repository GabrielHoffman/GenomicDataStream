% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GenomicDataStream.R
\name{atEndOfStream}
\alias{atEndOfStream}
\title{Detected if end of stream is reached}
\usage{
atEndOfStream(x)
}
\arguments{
\item{x}{\code{GenomicDataStream}}
}
\value{
if end of stream has been reached, return \code{TRUE}.  Else \code{FALSE}
}
\description{
Detected if end of stream is reached
}
\examples{
file <- system.file("extdata", "test.vcf.gz", package = "GenomicDataStream")

# initialize
obj <- GenomicDataStream(file, "DS", chunkSize = 5, initialize = TRUE)

# loop until break
while (1) {
  # get data chunk
  # data$X matrix with features as columns
  # data$info information about each feature as rows
  dat <- getNextChunk(obj)

  if (atEndOfStream(obj)) break

  print(dat$info)
}
}
