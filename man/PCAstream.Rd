% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PCAstream.R
\name{PCAstream}
\alias{PCAstream}
\alias{PCAstream,GenomicDataStream-method}
\alias{PCAstream,ANY-method}
\alias{PCAstream,SummarizedExperiment-method}
\title{Window-based Randomized SVD on data streamed from \code{GenomicDataStream}}
\usage{
PCAstream(
  x,
  k,
  ...,
  p = 7,
  s = 20,
  B = 64,
  threads = 4,
  scaleAndCenter = TRUE,
  shuffle = TRUE,
  verbose = FALSE
)

\S4method{PCAstream}{GenomicDataStream}(
  x,
  k,
  ...,
  p = 7,
  s = 20,
  B = 64,
  threads = 4,
  scaleAndCenter = TRUE,
  shuffle = TRUE,
  verbose = FALSE
)

\S4method{PCAstream}{ANY}(
  x,
  k,
  chunkSize = 1000,
  p = 7,
  s = 20,
  B = 64,
  threads = 4,
  scaleAndCenter = TRUE,
  shuffle = TRUE,
  verbose = FALSE
)

\S4method{PCAstream}{SummarizedExperiment}(
  x,
  k,
  chunkSize = 100,
  assay = "logcounts",
  ...,
  p = 7,
  s = 20,
  B = 64,
  threads = 4,
  scaleAndCenter = TRUE,
  shuffle = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{x}{\code{GenomicDataStream}}

\item{k}{integer; \cr
specifies the target rank of the low-rank decomposition. \eqn{k} should satisfy \eqn{k << min(m,n)}.}

\item{...}{other argument to control streaming}

\item{p}{integer, optional; \cr
number of additional power iterations (by default \eqn{p=7}).}

\item{s}{integer, optional; \cr
oversampling parameter (by default \eqn{s=20}).}

\item{B}{integer, optional; \cr
number of windows (by default \eqn{B=64}).}

\item{threads}{integer, optional; \cr
number of threads (by default \eqn{threads=4}).  Set to \code{min(threads, floor(nrow(chunks) / B))}}

\item{scaleAndCenter}{bool, optional; \cr
if \code{TRUE}, scale and center features}

\item{shuffle}{bool, optional; \cr
if \code{TRUE} (default) shuffle genomic regions, the next chunk is not in LD with the previous chunk}

\item{verbose}{string, optional; \cr
if \code{TRUE} (default is \code{FALSE}) print details}

\item{chunkSize}{number of features to read per chunk in \code{GenomicDataStream}}

\item{assay}{for \code{SummarizedExperiment} which \code{assay} to perform PCA on}
}
\value{
\code{PCAstream} returns a list containing the following three components:
\describe{
\item{d}{  array_like; \cr
          singular values; vector of length \eqn{(k)}.
}

\item{u}{  array_like; \cr
          left singular vectors; \eqn{(m, k)} or \eqn{(m, nu)} dimensional array.
}

\item{v}{  array_like; \cr
          right singular vectors; \eqn{(n, k)} or \eqn{(n, nv)} dimensional array. \cr
}
}
}
\description{
Window-based Randomized SVD on data streamed from \code{GenomicDataStream}
}
\details{
PCAstream implements the window-based Randomized SVD proposed by Li, et al. (2024)
}
\note{
The singular vectors are not unique and only defined up to sign.
If a left singular vector has its sign changed, changing the sign of the corresponding right vector
gives an equivalent decomposition.
}
\examples{
file <- system.file("extdata", "test.vcf.gz", package = "GenomicDataStream")

obj <- GenomicDataStream(file, "DS", chunkSize = 3)

res <- PCAstream(obj, k=5)

res

par(pty="s")
plot(res)

library(muscat)
library(SingleCellExperiment)
library(GenomicDataStream)

data(example_sce)
sce <- example_sce

# Normalize expression with log2 counts per million
prior.count <- 1
lib.size <- colSums2(counts(sce))
logcounts(sce) <- t(log2(t(counts(sce) + prior.count)) - log2(lib.size) + log2(1e6))

# PCA on SingleCellExperiment with assay logcounts
res <- PCAstream(sce, k=5, assay="logcounts")

res

}
\references{
\itemize{
 \item Li, Z., Meisner, J., & Albrechtsen, A. (2023). Fast and accurate out-of-core PCA framework for large scale biobank data. Genome Research, 33(9), 1599-1608. \doi{10.1101/gr.277525.122}.
}
}
\author{
Zilong Li \email{zilong.dk@gmail.com}
}
